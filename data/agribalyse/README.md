# Agribalyse Dataset Files

Place local Agribalyse CSV exports in this directory for import, for example:

```bash
npx tsx scripts/import-agribalyse.ts ./data/agribalyse/agribalyse.csv --version=3.2 --yes
```

CSV, XLSX, and ODS files in this folder are ignored by git.
