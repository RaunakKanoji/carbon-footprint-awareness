'use client';

export default function FoodMappingNotice() {
  return (
    <p className="rounded-lg border border-border-default bg-bg-base p-3 text-sm text-text-secondary">
      We found this food/product, but no reliable Agribalyse factor is mapped yet.
    </p>
  );
}
